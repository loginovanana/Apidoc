apiVersion: v1
kind: Service
metadata:
  name: apidoc-server
  namespace: apidoc
  labels:
    app: apidoc-server
spec:
  type: ClusterIP
  selector:
    app: apidoc-server
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  sessionAffinity: ClientIP
